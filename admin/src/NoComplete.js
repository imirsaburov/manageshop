import React, {Component} from 'react';
import PropTypes from 'prop-types';

class NoComplete extends Component {
    render() {
        return (
            <div>
                Hech narsa yoq
            </div>
        );
    }
}

NoComplete.propTypes = {};

export default NoComplete;